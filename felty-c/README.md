# Felty Ransomware - C Implementation (CachyOS Build)

## Prerequisites

### CachyOS / Arch Linux
```bash
# Install MinGW-w64 cross-compiler and build tools
sudo pacman -S mingw-w64-gcc python python-pip make

# Install Python cryptography library
pip install cryptography
